$git clone (enter your url).git

or

$ git remote add origin (enter your url).git

$git remote -v (view your connect)

$git pull origin master(add username and password with newbox)

$git status

$git add -- all

$git commit -m "enter your update"

$git push -u origin master

$ git config --global user.email

$ git config --global user.name "Your Name"




